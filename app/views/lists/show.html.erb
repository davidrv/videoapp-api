<p>
  <strong>Name:</strong>
  <%= @list.name %>
</p>

<p>
  <strong>User:</strong>
  <%= @list.user_id %>
</p>

<%= link_to 'Delete', @list, data: { 'turbo-method': :delete, 'turbo-confirm': 'Are you sure?' } %>
<%= link_to 'Edit', edit_list_path(@list)  %>
<%= link_to 'Back', root_path %>
<%= link_to 'New item', new_list_item_path(@list) %>
